
- name: Workstation
  hosts: localhost
  connection: local
  roles:
    - setup
    # Core/Common
    - hardware
    - terminal-tools
    - fonts
    - konsole
    - shell
    # Linux UI
    - virtual-desktops
    - shortcuts
    # Apps
    - firefox
    - obsidian
    - own-repos
